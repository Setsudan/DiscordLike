<UserControl x:Class="DiscordLikeChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscordLikeChatApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
         </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1"  Grid.Column="1" Text="Nom d'utilisateur" Margin="0,0,0,5" />
        <TextBox Grid.Row="2" Grid.Column="1" Name="UsernameTextBox" Margin="0,0,0,10" />

        <TextBlock Grid.Row="3" Grid.Column="1" Text="Mot de passe" Margin="0,0,0,5" />
        <PasswordBox Grid.Row="4" Grid.Column="1" Name="PasswordBox" Margin="0,0,0,10" />

        <Button Grid.Row="5" Grid.Column="1" Content="Se connecter" Width="100" Height="25" HorizontalAlignment="Center" Click="Button_Click"/>
    </Grid> 
</UserControl>
