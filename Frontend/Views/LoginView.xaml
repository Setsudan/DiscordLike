<UserControl x:Class="DiscordLikeChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscordLikeChatApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#2C2F33">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="#23272A" 
                Padding="30" 
                CornerRadius="12" 
                Width="350"
                BorderBrush="#7289DA"
                BorderThickness="2">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Titre -->
                <TextBlock Text="Bienvenue sur DiscordLikeChatApp" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           TextAlignment="Center" 
                           Margin="0,0,0,20" />

                <!-- Champ Nom d'utilisateur -->
                <TextBlock Text="Nom d'utilisateur :" Foreground="White" Margin="0,0,0,5" HorizontalAlignment="Left"/>
                <Border Background="#40444B" CornerRadius="8" Padding="5">
                    <TextBox Name="UsernameTextBox" 
                             Width="270" 
                             Height="30" 
                             FontSize="14"
                             Foreground="White"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="5"/>
                </Border>

                <!-- Champ Mot de passe -->
                <TextBlock Text="Mot de passe :" Foreground="White" Margin="0,10,0,5" HorizontalAlignment="Left"/>
                <Border Background="#40444B" CornerRadius="8" Padding="5">
                    <PasswordBox Name="PasswordBox" 
                                 Width="270" 
                                 Height="30" 
                                 FontSize="14"
                                 Foreground="White"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="5"/>
                </Border>

                <!-- Bouton Se connecter -->
                <Button Content="Se connecter" 
                    Width="280" 
                    Height="40" 
                    FontSize="16" 
                    FontWeight="Bold"
                    Foreground="White"
                    Background="#5865F2"
                    BorderBrush="Transparent"
                    Cursor="Hand"
                    Margin="15,15,15,5"
                    Click="Button_Click">           
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                    CornerRadius="8" 
                    BorderBrush="Transparent">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>


                <!-- Lien d'inscription -->
                <TextBlock Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0">
                    Pas encore inscrit ? 
                    <Hyperlink NavigateUri="register" RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock Foreground="#7289DA">Inscrivez-vous ici</TextBlock>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
