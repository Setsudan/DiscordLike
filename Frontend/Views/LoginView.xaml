<UserControl x:Class="DiscordLikeChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscordLikeChatApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Bienvenue sur DiscordLikeChatApp" FontSize="24" Margin="0,0,0,20" />
            <TextBlock Text="Nom d'utilisateur :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <TextBox Name="UsernameTextBox" Margin="0,0,0,10" Width="200" />
 
            <TextBlock Text="Mot de passe :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <PasswordBox Name="PasswordBox" Margin="0,0,0,10" Width="200" />

            <Button Content="Se connecter" Width="100" Height="25" HorizontalAlignment="Center" Click="Button_Click"/>

            <TextBlock Text="Pas encore inscrit ?" Margin="10,0,0,0" HorizontalAlignment="Center" />
            <TextBlock HorizontalAlignment="Center" Margin="10,0,0,0">
                  <Hyperlink NavigateUri="register" RequestNavigate="Hyperlink_RequestNavigate">
      Inscrivez-vous ici
  </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
