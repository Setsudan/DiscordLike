<UserControl x:Class="DiscordLikeChatApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscordLikeChatApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Barre latérale des serveurs -->
        <StackPanel Grid.Column="0" Background="#2C2F33">
            <Button Content="1" Width="50" Height="50" Margin="5" Background="#7289DA" Foreground="White" FontSize="24" Click="OnServerButtonClick" />
        </StackPanel>

        <!-- Barre latérale des canaux -->
        <StackPanel x:Name="ChannelStackPanel" Grid.Column="1" Background="#2F3136">
            <TextBlock Text="Serveur" Foreground="White" FontSize="20" Margin="10" />
            <Button Content="# général" Margin="10" Background="Transparent" Foreground="White" HorizontalAlignment="Left" Click="OnChannelButtonClick" />
            <!-- Bouton pour créer un nouveau canal -->
            <Button Content="Créer un nouveau canal" Margin="10" Background="Transparent" Foreground="White" HorizontalAlignment="Left" Click="OnCreateChannelButtonClick" />
        </StackPanel>

        <!-- Zone principale -->
        <Grid x:Name="MainGrid" Grid.Column="2" Background="#36393F">
            <TextBlock Text="Bienvenue sur le tableau de bord" FontSize="16" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>
