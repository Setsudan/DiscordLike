<UserControl x:Class="DiscordLikeChatApp.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Inscription" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center" />
            <TextBlock Text="Nom d'utilisateur :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <TextBox Name="UsernameTextBox" Margin="0,0,0,10" Width="200" />

            <TextBlock Text="Email :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <TextBox Name="EmailTextBox" Margin="0,0,0,10" Width="200" />

            <TextBlock Text="Mot de passe :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <PasswordBox Name="PasswordBox" Margin="0,0,0,10" Width="200" />

            <TextBlock Text="Rôles (séparés par des virgules, par défaut USER) :" Margin="0,0,0,5" HorizontalAlignment="Center" />
            <TextBox Name="RolesTextBox" Margin="0,0,0,10" Width="200" />

            <Button Content="S'inscrire" Width="100" Height="25" HorizontalAlignment="Center" Click="SignupButton_Click"/>
            <TextBlock HorizontalAlignment="Center" Margin="10,0,0,0">
                <Hyperlink NavigateUri="login" RequestNavigate="Hyperlink_RequestNavigate">
                    Déjà inscrit ? Connectez-vous ici
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
