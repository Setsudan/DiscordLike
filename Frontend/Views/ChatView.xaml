<UserControl x:Class="DiscordLikeChatApp.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Zone des messages -->
        <ListBox x:Name="MessagesListBox" Grid.Row="0" Background="#2F3136" Foreground="White" />

        <!-- Zone de saisie du message -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Background="#40444B">  
           <TextBox x:Name="MessageTextBox" Width="650" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />  
           <Button Content="Envoyer" Width="100" Margin="10" Click="OnSendButtonClick" />  
        </StackPanel>
    </Grid>
</UserControl>
